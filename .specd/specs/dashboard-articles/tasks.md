# Tasks — dashboard-articles

| id | role | files | depends-on | verify | acceptance |
|---|---|---|---|---|---|
| T1 | craftsman | `app/Http/Middleware/ArticleOwnership.php`<br>`routes/web.php` | - | `grep -q "ArticleOwnership" app/Http/Kernel.php` | Middleware checks Article.author_id = Auth::id(); 403 if not. Routes created under `auth` middleware group for all `/articles*`. GET /articles, GET /articles/create, POST /articles, GET /articles/{id}/edit, PUT /articles/{id}, DELETE /articles/{id} all defined. Route model binding on {id}. |
| T2 | craftsman | `app/Http/Controllers/ArticleDashboardController.php` | T1 | `php artisan route:list \| grep -c "ArticleDashboardController"` | Controller implements all 6 actions: index (paginate 10), create (render form), store (validate + create), edit (render form), update (validate + update), destroy (delete). Index filters by Auth::user()->articles()->paginate(10). Store/update validate title ≥3 chars, body ≥10 chars, publish toggle sets published_at. Destroy returns 403 if not owner (via middleware). |
| T3 | craftsman | `resources/views/articles/index.blade.php` | T2 | `curl -s http://127.0.0.1:8000/articles \| grep -q '<table\|<ul' && echo OK` | Lists user's articles: title, status (draft/published), created_at, edit/delete buttons. Pagination links shown (Laravel pagination). No articles message if list empty. Links to `/articles/create`. Delete form includes CSRF token + hidden `_method=DELETE`. Responsive table/card layout. |
| T4 | craftsman | `resources/views/articles/create.blade.php`<br>`resources/views/articles/edit.blade.php`<br>`resources/views/components/article-form.blade.php` | T2 | `curl -s http://127.0.0.1:8000/articles/create \| grep -q '<form\|<input' && echo OK` | Create view shows form heading "New Article", edit view shows "Edit Article". Form component accepts mode (create/edit), article (null for create). Fields: title (text input), excerpt (textarea), body (textarea), publish (checkbox). Form posts to POST /articles (create) or PUT /articles/{id} (edit). CSRF tokens present. Errors shown inline with field labels. Save button. |
| T5 | craftsman | `resources/css/app.css` | T4 | `curl -s http://127.0.0.1:8000/articles/create \| grep -q 'class=".*w-' && echo OK` | Dashboard pages styled with Tailwind CSS. Forms have proper spacing, inputs have labels, buttons have hover states. List table is responsive (card layout on mobile <768px). Create/edit forms centered with max-width. No horizontal scroll. Dark mode compatible via `@media (prefers-color-scheme: dark)`. |
