# Tasks — portfolio-landing

| id | role | files | depends-on | verify | acceptance |
|---|---|---|---|---|---|
| T1 | craftsman | `database/migrations/create_articles_table.php`<br>`app/Models/Article.php`<br>`database/migrations/create_github_repos_table.php`<br>`app/Models/GitHubRepo.php` | - | `php artisan migrate:fresh --seed 2>&1 \| grep -q "Database reset successfully"` | Migrations create tables with correct schema. Article model includes `published()` and `latest()` scopes. GitHubRepo model has name, description, url, stars, forks, language, updated_at. Both models persist correctly to SQLite. |
| T2 | craftsman | `config/services.php`<br>`.env.example`<br>`.env` | - | `php artisan config:cache && php artisan config:show services.github` | `config/services.php` stores `github.username` and `github.token` from env. `.env.example` documents `GITHUB_USERNAME` and `GITHUB_TOKEN`. Local `.env` has test values. Config retrieval via `config('services.github.token')` returns correct value. |
| T3 | craftsman | `app/Console/Commands/SyncGitHubRepos.php`<br>`app/Console/Kernel.php` | T1, T2 | `php artisan sync:repos && php artisan tinker -c "dd(GitHubRepo::count())"` | Command fetches user's public repos from GitHub API (via token in config). Stores/updates: name, description, url, stars, forks, language, updated_at. Result cached 24h. If API unavailable, command logs error, does not throw. Runs without failure. Creates/updates repos in database. Kernel schedules daily execution. |
| T4 | craftsman | `app/Http/Controllers/LandingController.php`<br>`routes/web.php` | T1, T3 | `curl -s http://127.0.0.1:8000/ \| grep -q '<h1' && echo OK` | Controller route GET / renders view. Fetches 5 latest published articles via Article::published()->latest()->limit(5). Fetches 5 latest repos via GitHubRepo::latest()->limit(5). If repo fetch fails (cache miss, API down), renders landing without repos section (no 500 error). Response contains article titles and repo names if data exists. View renders with 200 status. |
| T5 | craftsman | `resources/views/landing.blade.php`<br>`resources/views/components/article-card.blade.php`<br>`resources/views/components/repo-card.blade.php`<br>`resources/css/app.css` | T4 | `curl -s http://127.0.0.1:8000/ \| grep -c '<article' && curl -s http://127.0.0.1:8000/ \| grep -c 'rel="noopener noreferrer"'` | Landing view renders hero section with title/subtitle, articles section (5 cards), repos section (5 cards), footer with contact CTA. Article cards show: title, excerpt, link to article. Repo cards show: name, description, stars, forks, language, GitHub link. All external links have `target="_blank" rel="noopener noreferrer"`. Page responsive 320px–2560px viewport width, no horizontal scroll. Tailwind CSS applied, mobile-first design. View does not log or expose GitHub token. |
